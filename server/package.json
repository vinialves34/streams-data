{
  "name": "streams-json",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node --watch index.js",
    "request": "curl -N localhost:3000",
    "request:options": "curl -i -X OPTIONS -N localhost:3000"
  },
  "dependencies": {
    "csvtojson": "^2.0.10"
  }
}
